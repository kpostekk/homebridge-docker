{
  "pluginAlias": "Docker",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "host": {
        "title": "Host",
        "type": "string",
        "required": true,
        "placeholder": "http://192.168.1.100",
        "typeahead": {
          "source": [
            "http://192.168.1.",
            "http://127.0.0.1",
            "http://localhost"
          ]
        },
        "description": "Address of Docker host.",
        "format": "uri"
      },
      "port": {
        "title": "Port",
        "type": "integer",
        "required": true,
        "default": 2375,
        "placeholder": "2375"
      },
      "secureDelayTimeout": {
        "title": "Timeout for miss clicks",
        "type": "integer",
        "required": true,
        "default": 0,
        "placeholder": "5000",
        "description": "Time window for miss clicks in milliseconds, 0 and lower values disable this experimental feature."
      }
    }
  }
}